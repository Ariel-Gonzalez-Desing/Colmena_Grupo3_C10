<!DOCTYPE html>
<html lang="en">

    <%-include('../partials/head')%>

<body class="admin">

   <%-include('../partials/header')%>
   
    <div class="cabecera">
      <h3>Administrador de Usuarios</h3>    
       
    </div>
    <table summary="Listado de usuarios">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Imagen</th>
            <th scope="col">Email</th>
            <th scope="col">Nombre</th>
            <th scope="col">Apellido</th>            
            <th scope="col">Rol</th>
          </tr>
        </thead>
        <tbody>
            <% users.forEach((user, index) => { %>            
          <tr>
            <th scope="row"><%= index + 1 %></th>
            <td><a href=""><img src="/images/users/<%= user.avatar%>" alt="imagen de usuario"></a></td>
            <td><%= user.email %></td>
            <td><%= user.firstName %></td>
            <td><%= user.lastName %></td>
            <td><div class="iconos">

                <form action="/adminUsers/ <%= user.id %>?_method=PUT" method="POST">
                  <!-- Toggle admin/user -->
                   <label class="switch"> 
                    <% if (user.rolId === 2) { %>
                      <input type="checkbox" id="rolEdit" id="rolEdit" checked>              
                   <% } else { %>
                    <input type="checkbox" id="rolEdit" id="rolEdit">   
                     <% } %>  
                  <div class="slider round"> 
                  <!--ADDED HTML -->
                   <span class="on">Admin</span>
                  <span class="off">User</span>
                  <!--END-->
                  </div>
                </label>
              </form>             

              <!-- <form action="/adminUsers/<%= user.id %>?_method=PUT" method="POST">  
                <% if (user.rolId === 1) { %>
                   <input type="radio" name="rolEdit" id="rolEdit" value="1" checked> User </input><br><br>                 
                <% } else { %>
                   <input type="radio" name="rolEdit" id="rolEdit" value="1" > User </input><br><br>
                  <% } %>  

                <% if (user.rolId === 2) { %>
                    <input type="radio" name="rolEdit" id="rolEdit" value="1" checked> Admin </input>
                   
                <% } else { %>
                    <input type="radio" name="rolEdit" id="rolEdit" value="1" > Admin </input>
                    <% } %>  
                              -->
                         
            <button class="buttonEditRol" id="buttonEditRol">Cambiar Rol</button>
          </form>

            <form action="/delete/<%= user.id %>?_method=DELETE" method="POST">
              <button onclick="return confirm('¿Desea eliminar el usuario permanentemente?');"><i class="fas fa-trash-alt"></i></button>
            </form>
            
            </div></td>
          </tr>
          <% }) %>
        </tbody>
        
      </table>
      <script src="/javascripts/admin.js"></script>
</body>
</html>